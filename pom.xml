<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <version>0.9.4</version>
    <groupId>io.mszymanski</groupId>
    <artifactId>orknux-server</artifactId>
    <packaging>pom</packaging>

    <!--
        One deployable, built from modules that cannot reach into each other.
        `app` is the service people talk to; `connection` and `execution` are
        what it is built on, and neither may depend on `app` — where they need
        something it owns, they declare an interface and it implements it.
    -->
    <modules>
        <module>modules/connection</module>
        <module>modules/execution</module>
        <module>app</module>
    </modules>

    <properties>
        <java.version>25</java.version>
        <kotlin.version>2.4.10</kotlin.version>
        <spring-boot.version>4.0.7</spring-boot.version>
        <temporal.version>1.38.0</temporal.version>
        <slack.version>1.45.3</slack.version>
        <graaljs.version>25.0.4.1</graaljs.version>
        <db-scheduler.version>15.6.0</db-scheduler.version>
        <sshd.version>2.18.0</sshd.version>
        <eddsa.version>0.3.0</eddsa.version>
        <sqlite.version>3.51.0.0</sqlite.version>
        <openai.version>4.52.0</openai.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>2.0.0</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>io.mszymanski</groupId>
                <artifactId>orknux-connection</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.mszymanski</groupId>
                <artifactId>orknux-execution</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-reflect</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <!-- So `./mvnw spring-boot:run -pl app` resolves the plugin from here. -->
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
        <sourceDirectory>${project.basedir}/src/main/kotlin</sourceDirectory>
        <testSourceDirectory>${project.basedir}/src/test/kotlin</testSourceDirectory>
        <plugins>
            <!--
                Declared here so that `spring-boot:run -pl app -am` works.

                Running the app alone resolves its siblings from the local
                repository, so a change in a module is invisible until something
                installs it — the build fails on a symbol that is right there in
                the source. `-am` fixes that by building them in the same
                reactor, but it also invokes the goal on every project in it, and
                a module with no main class fails.

                So the goal is a no-op everywhere except the app, which turns it
                back on. No executions: nothing is bound to a phase by this.
            -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.jetbrains.kotlin</groupId>
                <artifactId>kotlin-maven-plugin</artifactId>
                <version>${kotlin.version}</version>
                <executions>
                    <execution>
                        <id>compile</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>test-compile</id>
                        <phase>test-compile</phase>
                        <goals>
                            <goal>test-compile</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <jvmTarget>${java.version}</jvmTarget>
                    <args>
                        <arg>-Xjsr305=strict</arg>
                        <arg>-Xannotation-default-target=param-property</arg>
                    </args>
                    <compilerPlugins>
                        <plugin>spring</plugin>
                        <plugin>jpa</plugin>
                    </compilerPlugins>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>org.jetbrains.kotlin</groupId>
                        <artifactId>kotlin-maven-allopen</artifactId>
                        <version>${kotlin.version}</version>
                    </dependency>
                    <dependency>
                        <groupId>org.jetbrains.kotlin</groupId>
                        <artifactId>kotlin-maven-noarg</artifactId>
                        <version>${kotlin.version}</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>

</project>
